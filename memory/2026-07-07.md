# 2026-07-07 Session Log

## Time
2026-07-07 UTC+8 11:00-14:30

## Context
- Cursor IDE Agent workspace: C:\Users\Hasee\.qclaw\workspace\get_jobs
- Goal: Execute plan "Post MGU PhD notes + rate-limit probe"

---

## What was done

### Step 1 - Auth verification
- dev server 8080: 200 {status:ok}
- Auth smoke: signup 201, login 200 OK
- email-validator==2.3.0 installed

### Step 2 - MGU PhD template + images
- xiaohongshu-saas/data/templates/mgu_phd.json created
- xiaohongshu-saas/data/images/mgu_phd_{1,2,3}.jpg PIL generated (800x800)

### Step 3 - Seed account + task
- scripts/seed_mgu_phd.py written and run
- Account acc_676072139 created (stage=normal, cookie_path=data/cookies/acc_676072139.json)
- Task 2 created (name=MSU-PhD-probe, kind=probe, status=draft)
- Cookie harvested: msu-test.json copied to acc_676072139.json

### Step 4 - rate_limit_probe.py fixes
- OUT_DIR = REPO_ROOT / logs = xiaohongshu-saas/logs
- TASK_NAME = correct middle dot string
- async def load_account_and_task + await session.get/execute
- channel_registry.get not registry.get
- _Acct class stand-in for adapter.publish

### Step 5 - Git commits (4)
- Commit 1: feat(template) - 9 files (mgu_phd template + images + seed script)
- Commit 2: feat(xhs-saas) - 5 files (alembic + seed_msu_invitation)
- Commit 3: fix+xhs - 7 files (console_gui + adapters + wix)
- Commit 4: build - MSI only (exe too large)

### Step 6c - MSI build
- build_msi.ps1 -Version 0.6.4 -SkipVerify SUCCEEDED
- Output: installer/output/xhs-saas-console-0.6.4.msi (159.2 MB)

### dev-browser install (failed)
- npm install -g dev-browser: EPERM file lock
- npm install --prefix .: SSL cert verification failed (corporate network SSL interception)
- curl -L -k: exit 35 (same SSL issue)

### Git push (not completed)
- git push origin main started, user interrupted (195MB exe upload too slow)

### Cookie harvest
- User scanned QR code; cookie saved to data/cookies/msu-test.json (today 9:56)
- Copied to data/cookies/acc_676072139.json

---

## Still todo

1. rate_limit_probe.py was running but Chromium did not return results (OOM/AMSI crash?)
   - Next: cd xiaohongshu-saas && python scripts/rate_limit_probe.py --rungs 30,20 --headed
2. git push origin main not completed
3. GitHub Release v0.6.4 not created (MSI built but not uploaded)

---

## Technical debt

### PS environment: Write tool writes UTF-16 by default
- Fix: use Python open() or .NET [System.IO.File]::WriteAllText
- py_compile fails on UTF-16 files (null bytes)

### Bytecode cache
- Stop-Process + Remove-Item __pycache__ after .py changes

### app/ast.py shadows stdlib ast module
- Exists at xiaohongshu-saas/app/ast.py
- python -c "import ast" picks up wrong module from xhs-saas cwd

### XHS XPath adapters (xiaohongshu/app/channels/xiaohongshu/) are fragile
- SPA DOM changes break XPath selectors

### PS AMSI crashes periodically
- AccessViolationException in ScanContent
- Likely memory pressure in Cursor Agent PS host

---

## Next session

- [ ] git push origin main
- [ ] Create GitHub Release v0.6.4 (upload MSI)
- [ ] Run rate_limit_probe.py --rungs 30,20 --headed
- [ ] Fill docs/rate_limit_probe_findings.md after probe
- [ ] dev-browser install (wait for network fix)
- [ ] Stamp alembic migration into DB