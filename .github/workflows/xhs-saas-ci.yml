name: xhs-saas-ci

on:
  push:
    branches: [main]
    paths:
      - 'xiaohongshu-saas/**'
      - '.github/workflows/xhs-saas-ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'xiaohongshu-saas/**'
      - '.github/workflows/xhs-saas-ci.yml'

jobs:
  xhs-saas-backend:
    name: xhs-saas backend (ruff + pytest)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: xiaohongshu-saas
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: xiaohongshu-saas/pyproject.toml
      - run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - run: ruff check .
      - run: pytest -q tests

  xhs-saas-console:
    name: xhs-saas console (tsc + vitest + vite build)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: xiaohongshu-saas/web/console
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: xiaohongshu-saas/web/console/package-lock.json
      - run: npm ci
      - run: npm test -- --run
      - run: npm run build

  pbp-api:
    name: donor-screener-pbp (pytest)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: donor-screener-pbp
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: donor-screener-pbp/requirements.txt
      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - run: pytest -q tests

  lakehouse-api:
    name: data-lakehouse (pytest)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: data-lakehouse
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: data-lakehouse/pyproject.toml
      - run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - run: pytest -q tests